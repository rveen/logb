{
  "name": "logb-viewer-web",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Browser UI for the Logb viewer. Built output is committed to dist/ and embedded in the Go binary, so building this is not required to run logbview.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.24.0",
    "uplot": "^1.6.32"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.4.3"
  }
}
